def fibonacci_generator(n):
    a, b = 0, 1
    for _ in range(n):
        yield a
        a, b = b, a + b

# Example: Generate the first 15 Fibonacci numbers
result_generator = list(fibonacci_generator(15))
print(result_generator)
